import 'package:flutter/material.dart';

class UpsScreen extends StatelessWidget {
  const UpsScreen({super.key});

  @override
  Widget build(BuildContext context) {
      return Scaffold(appBar: AppBar(title: Text("UPS")));
  }
}
