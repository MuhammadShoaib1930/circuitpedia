import 'package:flutter/material.dart';

class CapacitorScreen extends StatelessWidget {
  const CapacitorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Capacitor")));
  }
}
